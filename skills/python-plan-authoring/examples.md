# Examples

- Positive: Freeze a Python API refactor into goal, scope, non-goals, locked decisions, affected areas, thin SDD fields, thin TDD fields, validation commands, and explicit async status.
- Positive: Stop and ask when the public API shape or error-handling stance is still unclear enough to change scope materially.
- Negative: Emit a generic todo list, require repo-visible `.plan.md`/`.step.md` artifacts as part of the skill contract, or promise exact file edits without grounded codebase evidence.
