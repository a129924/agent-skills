# Platform Coupling Inventory

## Scope and Result

- Topic: `platform-coupling-inventory`
- Source-of-truth order used:
  1. `analysis/codex-migration-runway/technical-spec.md`
  2. `analysis/codex-migration-runway/requirements.md`
  3. `plan/positioning-freeze/positioning-freeze.plan.md`
- Writable scope used: `docs/migration/platform-coupling-inventory.md` only
- Inventory result: no wording drift found against the frozen `positioning-freeze` boundary
- Independent plan review result:
  - No repo-visible topic plan is cited here as an authority input for this inventory document.
  - This inventory therefore does not reference a separate plan-review verdict in this file.

## Classification Rules

### Primary dependency classes

- `workflow dependency`
- `artifact dependency`
- `runtime/tooling blocker`

### Secondary dependency classes

- `contract dependency`
- `source/path dependency`

## Findings

| Exact file/path locator | Current role | Target role | Primary dependency classes | Secondary dependency classes | Risk | Required action | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `AGENTS.md:12-27` | Governance summary fixes current state: `AGENTS.md` is canonical, `.github/skills/` is current active path, `skills/` is target-only | Keep as governance summary until a later path-transition phase intentionally rewrites the active-path statement | `artifact dependency` | `source/path dependency` | Medium | Preserve as frozen baseline input for later migration planning; do not cut over from this file in inventory phase | This file is a repo-wide authority anchor, so any path transition must update it in lockstep with the true operating state. |
| `docs/repo-positioning.md:14-24`, `31-42`, `55-74`, `92-100` | Positioning authority defines current state, target architecture, migration boundary, and names future blockers | Remain the authoritative positioning artifact until a dedicated transition phase updates current-vs-target wording | `artifact dependency`, `workflow dependency` | `source/path dependency`, `contract dependency` | High | Use as the top-level migration boundary source when sequencing future phases for creator/reviewer/template and runtime/tooling alignment | This document already states that `.github/skills/` should become a projection only after separate migration updates creator, reviewer, template, runtime/tooling, and installer assumptions. |
| `README.md:3-9`, `20-41`, `58-66`, `116-124`, `182-240` | Human-facing summary mirrors current `.github/skills/` activity, target `skills/` architecture, workflow ownership, and stable-library catalog | Stay a summary artifact until later migration can safely re-point documented workflow and catalog semantics | `artifact dependency` | `source/path dependency` | Medium | Defer README rewrites to a later phase after authoritative path-transition decisions are frozen | README is broad fan-out documentation; updating it early would create false current-state claims. |
| `.github/copilot-instructions.md:15-31`, `33-52`, `166-177`, `179-241` | GitHub/Copilot always-on mirror binds current skill storage to `.github/skills/<skill-name>/`, mandates creator/reviewer subagents, and elevates the two planning-spine skills for the analysis layer | Remain the active always-on workflow mirror until a later migration phase updates path and lifecycle instructions together | `workflow dependency`, `artifact dependency` | `contract dependency`, `source/path dependency` | High | Plan a dedicated follow-up phase to re-point always-on instructions only after creator/reviewer/template and runtime/tooling assumptions are ready | This file is both path-coupled and role-coupled; changing only path text without contract realignment would break lifecycle guidance. |
| `analysis/codex-migration-runway/requirements.md:29-37`, `51-61`, `63-78`, `83-92`, `124-130` | Frozen runway baseline names `.github/skills/business-intent-alignment` and `.github/skills/business-to-technical-translation` as mandatory planning-spine inputs and forbids path cutover in the runway baseline | Continue as frozen business baseline until later migration topics deliberately replace the planning spine or its path references | `workflow dependency`, `artifact dependency` | `contract dependency`, `source/path dependency` | High | Keep this artifact unchanged and treat planning-spine path changes as a downstream plan topic, not an implementation-side adjustment | This row is intentionally not a primary `runtime/tooling blocker`; its coupling is planning/workflow and artifact authority. |
| `analysis/codex-migration-runway/technical-spec.md:24-37`, `74-106`, `120-137`, `156-178`, `200-217` | Technical baseline translates the runway into ordered planning-spine steps and explicit implementer stop conditions | Remain the execution-facing analysis baseline until a later migration topic updates the planning architecture deliberately | `workflow dependency`, `artifact dependency` | `contract dependency`, `source/path dependency` | High | Preserve as analysis authority; if planning-spine path changes are needed later, update through a dedicated planning/spec revision topic | This row is also intentionally not a primary `runtime/tooling blocker`; it is a planning-contract dependency. |
| `plan/positioning-freeze/positioning-freeze.plan.md:34-78`, `98-145`, `147-175` | Frozen neighboring plan defines the positioning boundary, preserves current-vs-target wording discipline, and constrains migration work to inventory and sequencing inputs rather than active-path cutover | Remain a repo-visible boundary artifact until a later transition phase intentionally changes the frozen positioning assumptions | `workflow dependency`, `artifact dependency` | `contract dependency`, `source/path dependency` | High | Use this neighboring frozen plan only as boundary context; keep the inventory inventory-only and avoid introducing new path-transition claims from this document | This replaces the prior phase-local plan reference with an adjacent repo-visible artifact that still explains why this inventory must stay within the frozen migration boundary. |
| `.github/skills/business-intent-alignment/SKILL.md:3`, `14-23`, `41-60`, `66-79`, `117-127` | Planning-spine skill that produces `analysis/<topic>/requirements.md` and gates technical translation | In a future migrated architecture, still a planning-spine skill, but only after a separate path-transition updates how the active library locates it | `workflow dependency`, `artifact dependency` | `contract dependency`, `source/path dependency` | High | Keep classified as planning-spine dependency; later migration must update the path and any library-discovery surfaces without changing its planning role | Required special-focus classification: primary `workflow dependency` + `artifact dependency`; secondary `contract dependency` + `source/path dependency`; excluded primary `runtime/tooling blocker`. |
| `.github/skills/business-to-technical-translation/SKILL.md:3`, `7-27`, `45-61`, `97-124`, `126-129` | Planning-spine skill that produces `analysis/<topic>/technical-spec.md` and routes vague baselines back to business alignment | In a future migrated architecture, still a planning-spine skill, but only after a separate path-transition updates active-library discovery and downstream references | `workflow dependency`, `artifact dependency` | `contract dependency`, `source/path dependency` | High | Keep classified as planning-spine dependency; do not treat it as a runtime/tooling blocker in this phase | Required special-focus classification matches the prompt: primary `workflow dependency` + `artifact dependency`; secondary `contract dependency` + `source/path dependency`; excluded primary `runtime/tooling blocker`. |
| `.github/skills/agent-skill-creator/SKILL.md:46-65`, `71-78`; `.github/skills/agent-skill-creator/folder-contract.md:1-27`, `76-91`, `175-209` | Creator lane hard-codes new skill output to `.github/skills/<skill-name>/` and defines repository-governance metadata consumed by review flow | Future path-transition phase must re-point creator output and revalidate YAML/body contract expectations without changing meaning mid-phase | `workflow dependency`, `artifact dependency` | `contract dependency`, `source/path dependency` | High | Create a dedicated creator-lane transition phase before any library cutover; inventory only, no contract edits here | This is a direct blocker for path migration because draft location and folder contract remain anchored to `.github/skills/`. |
| `.github/skills/agent-skill-reviewer/review-checklist.md:77-100`, `149-159`, `180-184` | Reviewer lane enforces lifecycle independence, path alignment, and complexity/risk checks against current skill-folder semantics | Future path-transition phase must update review criteria and path expectations in lockstep with creator/template changes | `workflow dependency`, `artifact dependency` | `contract dependency`, `source/path dependency` | High | Sequence reviewer-lane alignment with creator/template updates; do not cut over active library path until review criteria point at the new source of truth | Reviewer logic is not tooling-driven, but it is a hard workflow gate for any future migration. |
| `.github/skills/agent-skill-template/template.md:5-23`, `25-75`; `.github/skills/agent-skill-template/folder-contract.md:1-18`, `29-52`, `72-74` | Template lane scaffolds the canonical skill folder under `.github/skills/<skill-name>/` and mirrors repository policy | Future transition must re-point the scaffold contract only after creator/reviewer path semantics are ready | `workflow dependency`, `artifact dependency` | `contract dependency`, `source/path dependency` | High | Add a template-lane transition phase after creator/reviewer sequencing is defined | Template coupling is structural; leaving it unchanged while cutting over would regenerate old-path content. |
| `.github/skills/python-blueprint-authoring/references/required-skills-validation.md:6-22`, `24-37`, `89-109` | Greenfield blueprint authoring requires exact current-library resolution under `.github/skills/<skill-name>/SKILL.md` | Future transition must swap exact-name validation to the new active library root once that root is authoritative | `workflow dependency` | `contract dependency`, `source/path dependency` | High | Record as a downstream validation-surface update in a future greenfield-lane phase | This is a strict locatability gate and will reject future-path names until the rule is explicitly changed. |
| `.github/skills/python-project-init-greenfield/references/baseline-generation-rules.md:6-25`, `85-121`, `151-163` | Greenfield runtime baseline generation materializes `.github/skills/` into target repos and requires the canonical `sense-env-scaffold` CLI path under `.github/skills/` | Future transition must update baseline layout, installed-skill copy rules, and acceptance handoff together | `runtime/tooling blocker`, `artifact dependency` | `contract dependency`, `source/path dependency` | High | Plan a dedicated runtime/tooling transition phase for greenfield baseline generation before any active-path cutover | This row is a true runtime/tooling blocker because it drives generated repo layout and execution commands, not just documentation. |
| `.github/skills/python-project-retrofit/SKILL.md:43`, `116`, `139`; `.github/skills/python-project-retrofit/references/sensing-delta-contract.md:124` | Retrofit execution lane hard-codes `python3 .github/skills/sense-env-scaffold/scripts/sense_env.py --mode acceptance --contract-file retrofit-plan.md` in trigger, process, verification, and acceptance handoff guidance | Future transition must preserve or explicitly replace this acceptance command contract everywhere the retrofit executor and its downstream review surfaces expect it | `runtime/tooling blocker`, `artifact dependency` | `contract dependency`, `source/path dependency` | High | Record a dedicated retrofit runtime/tooling follow-up before changing the active skill source path or sense-env CLI location | This is a real downstream acceptance handoff contract, not explanatory prose: the exact executable path and arguments are part of the retrofit completion gate. |
| `.github/skills/sense-env-scaffold/SKILL.md:15`, `27-30`, `53-67`, `84-100`, `121-129` | Runtime sensing skill hard-codes `.github/skills/sense-env-scaffold/scripts/sense_env.py` as the executable CLI path and treats the local runtime package as fixed tooling | Future transition must move or alias the CLI path without breaking manifest, exit-code, and contract behavior | `runtime/tooling blocker` | `contract dependency`, `source/path dependency` | High | Add a tooling-specific transition topic covering CLI path, local runtime package assumptions, and downstream callers | This is an actual executable-path dependency; inventorying it without repair is correct for this phase. |
| `.github/skills/plan-step-tracker/SKILL.md:42-64`, `86-100` | Workflow gate helper requires the exact command path `python .github/skills/plan-step-tracker/scripts/step_tracker.py <operation> <topic>` | Future transition must either preserve this CLI path via compatibility layer or update all call sites and documentation together | `runtime/tooling blocker` | `contract dependency`, `source/path dependency` | High | Record a dedicated step-tracker tooling follow-up before changing the active source path | This row is a true runtime/tooling blocker because the path is part of the command contract and exit-code gate behavior. |
| `.github/skills/copilot-instructions-init/SKILL.md:13-17`, `18-31`, `60-90`, `124-168` | High-risk generator depends on `.github/skills/` summary as an input fingerprint and writes target-project `.github/copilot-instructions.md` based on installed-skill inventory | Future transition must redefine stale-check fingerprints and installed-skill discovery once the active library path changes | `runtime/tooling blocker`, `artifact dependency` | `contract dependency`, `source/path dependency` | High | Add a follow-up generator/runtime topic that updates stale-fingerprint logic and instruction-generation assumptions after library-path decisions are frozen | This row couples generation logic to both `.github/skills/` inventory and `.github/copilot-instructions.md` output policy. |

## High-Risk Summary

- Planning spine remains anchored to `.github/skills/business-intent-alignment` and `.github/skills/business-to-technical-translation` across governance, analysis, and neighboring boundary artifacts.
- Creator, reviewer, and template lanes are all path-coupled to `.github/skills/`; they are workflow blockers for any future cutover, but this phase does not authorize contract edits.
- Runtime/tooling blockers are concrete and executable, not speculative:
  - `sense-env-scaffold` CLI path
  - `plan-step-tracker` CLI path
  - `python-project-init-greenfield` baseline layout and acceptance handoff
  - `python-project-retrofit` acceptance handoff contract
  - `copilot-instructions-init` stale-fingerprint and installed-skill summary logic

## Reviewer Lane Decision

- This inventory file does not cite a separate repo-visible topic plan review result.
- `agent-skill-reviewer` was not used in this phase.
- Reason: this phase does not modify any skill contract, creator/reviewer/template contract, or skill folder content. The inventory identifies later reviewer-lane dependencies, but there is no in-scope skill artifact to send through `agent-skill-reviewer` now, and this document does not rely on a separately cited topic-plan review verdict.
