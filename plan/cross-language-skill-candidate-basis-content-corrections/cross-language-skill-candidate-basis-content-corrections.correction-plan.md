# Cross-Language Skill Candidate Basis Content Corrections — Recovery Record

## Correction Classification

- **Severity:** `high`.
- **Frozen direction:** rebuild the feature branch from `origin/dev` and
  re-establish planning truth before any future six-cell candidate-basis edit.
- **Reason:** the prior branch history combined planning, implementation,
  review-state claims, and publication routing in a way that invalidated
  confidence in phase and role separation. The removed implementation is
  treated as suspect and is not carried into this recovery baseline.

## Historical-Truth Boundary

- This correction plan records why recovery is required and which prospective
  route repairs it. It is historical truth only.
- The parent plan remains the current execution-facing contract; the parent
  step remains progression truth. Neither may be overridden by this record.
- This record contains no reviewer verdict, approval, review log, summary, or
  completion claim.

## Frozen Repair Direction

- Start from `origin/dev`; retain no prior implementation or review-state claim
  on the recovery branch.
- Establish exactly the parent plan, parent step, this correction plan, and
  this correction step before independent Plan-Reviewer review.
- Preserve the parent plan's sole future existing-file write and its exact six
  `可攜核心` cell contract. No candidate-basis edit is authorized at this stage.
- Preserve every parent-plan exclusion: the 11 candidates, four groups, other
  five rows, evidence, appendices, names, paths, `skills/**`, `agents/**`,
  `.github/**`, `.codex/**`, README, VERSION, release, and language-validation
  claims remain outside this correction.

## Prospective Recovery Chronology

1. Independent Plan-Reviewer reviews the four baseline artifacts and returns
   its single JSON verdict.
2. Only an `approved` verdict permits independent Implementer work on the six
   cells.
3. Independent Reviewer reviews the future bounded document change.
4. Main Agent performs Phase 4.5 alignment, then publication only under STOP
   POINT 1; human review and STOP POINT 2 remain separate gates.

## Closure Conditions

- This correction remains open until parent current truth is synchronized with
  any approved repair and all required downstream reviews pass.
- A review log is created only if reviewer feedback controls routing or
  multi-round rework; a summary is created only when topic-close conditions
  require it. Neither is created in this baseline.
- Correction closure is not granted by this artifact; a Planner confirms it
  only after the required parent sync and reviews.
